name: text-to-image
type: gradio-space
sdk: gradio

metadata:
  space_id: Chris4K/text-to-image
  original_url: https://huggingface.co/spaces/Chris4K/text-to-image

deploy:
  targets:
    - public-repo
    - huggingface

build:
  command: bash build.sh

test:
  command: echo "Tests not yet implemented"
